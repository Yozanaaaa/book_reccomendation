<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <style>
    
    body {
      background-color: rgb(123, 120, 120);
      color: white;
    }
    /* Navbar Styling */
.navbar {
  background-color: #2b2e2f; /* Change to any color you like */
  border: 1px solid #333;    /* Add border around navbar */
      /* Optional: rounded corners */
  display: flex;             /* Use flexbox to align the links */
  justify-content: space-between; /* Space between the links */
  padding: 5px;
}

/* Navbar Links Styling */
.navbar ul {
  list-style-type: none;
  padding: 0;
  margin: 0;
  display: flex;
  justify-content: space-around;
}

.navbar ul li {
  margin: 0;
}

.navbar ul li a {
  color: white;             /* Link color */
  text-decoration: none;    /* Remove underline */
  padding: 10px 15px;       /* Add padding to links */
  display: block;
}
.nav-link {
  color: white;             /* Link color */
  text-decoration: none;    /* Remove underline */
  padding: 10px 20px;       /* Add space inside the link (top-bottom, left-right) */
  display: block;
  margin-right: 20px;       /* Add space between links */
}

.nav-link:last-child {
  margin-right: 0;          /* Remove right margin from the last link */
}

.nav-link:hover {
  background-color: #181413; /* Change background on hover */
}
.navbar ul li a:hover {
  background-color: #191a19; /* Change background on hover */
}

    
    .card {
      background-color: #222;
      border-radius: 10px;
      padding: 10px;
      text-align: center;
    }
    .card img {
      width: 100%;
      height: 300px;
      object-fit: cover;
      border-radius: 10px;
    }
    .form-control {
      padding: 10px;
      font-size: 16px;
    }
    
    
  </style>
</head>
<nav class="navbar navbar-expand-lg navbar-dark">
    
      <a class="nav-link">My Book Recommender</a>
        <div class="navbar">
          <a class="nav-link" href="/">Home</a>
          <a class="nav-link" href="/recommend">Recommend</a>
          <a class="nav-link" href="/contact">Contact</a>
          <a class="nav-link" href="#">
            <img src="<%= user.image %>"  style="width: 20px; height: 20px; border-radius: 60%;">
          </a>
          
          <a class="nav-link" href="/logout">Log Out</a>
        </div>
      </nav>
     
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <!-- Show Home only if the user is logged in -->
            
           
            
          </ul>
          
      </div>
    </div>
</nav>
<body>

  <h1>Welcome, <%= user.displayName %>!</h1>
  <!-- Profile Image (head) -->
 
  <div class="container my-5">
    <div class="card p-4 shadow-lg border-0">
      <h1 class="mb-4 text-center">Recommend Books</h1>
      <form action="/recommend_books" method="post" class="mb-5">
        <div class="input-group">
          <input name="user_input" type="text" class="form-control" placeholder="Enter a book name...">
          <div class="input-group-append">
            <input type="submit" class="btn btn-warning btn-lg">
          </div>
        </div>
      </form>
  
      <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-4">
        {% if data %}
          {% for i in data %}
            <div class="col">
              <div class="card h-100 border-0 shadow-sm">
                <img class="card-img-top" src="{{ i[2] }}" alt="Book Cover">
                <div class="card-body">
                  <h5 class="text-dark">{{ i[0] }}</h5>
                  <p class="text-muted">by {{ i[1] }}</p>
                </div>
              </div>
            </div>
          {% endfor %}
        {% endif %}
      </div>
    </div>
  </div>
  

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
